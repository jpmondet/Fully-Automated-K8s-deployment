---
# Global variables that can 
# be included in a task with 
#  include_vars: "{{ playbook_dir }}/config.yaml"
################################################

#
## Essential network variables ##
#
master_ip: 11.11.11.11
# Why 85 ? Because it looks like 8s in k8s ;-)
service_ip: 85.85.0.0/16    
cluster_dns: 85.85.85.85  
cluster_cidr: 172.168.0.0/16
node_mask: 24   # Size of the subnet (in the cluster_cidr) per node

#
## Misc for certificates
#
cluster_name: MyK8sCluster
country: the
locality: automated
organization: way
organizationalUnit : in
state: ansible

